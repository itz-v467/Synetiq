export default function MeetingsPage() {
  return (
    <section>
      <h1 className="font-display text-display-xl text-primary">Meetings</h1>
      <p className="mt-2 text-body-lg text-muted">Connect this view to `/api/v1/meetings` when you wire data.</p>
    </section>
  );
}
